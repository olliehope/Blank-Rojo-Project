{
  "name": "blank-rojo-project",
  "projectName": "Blank-Rojo-Project",
  "version": "0.1.0",
  "description": "Blank-Rojo-Project Roblox project setup.",
  "main": "index.js",
  "scripts": {
    "init": "node tools/initProject.js",
    "build:rojo": "node tools/genFeatureTree.js",
    "watch:rojo": "chokidar \"src/**/*\" -c \"node tools/genFeatureTree.js\" --initial",
    "sourcemap": "node tools/genFeatureTree.js && rojo sourcemap default.project.json --output sourcemap.json",
    "format": "stylua src",
    "lint:style": "stylua --check src",
    "check": "node tools/genFeatureTree.js && stylua --check src && rojo sourcemap default.project.json --output sourcemap.json",
    "serve": "node tools/genFeatureTree.js && rojo serve default.project.json",
    "build": "node tools/genFeatureTree.js && rojo build default.project.json -o place.rbxlx",
    "wally:install": "npm run wally:types",
    "wally:types": "wally install && npm run wally:types:apply",
    "wally:types:apply": "node tools/genFeatureTree.js && rojo sourcemap default.project.json --output sourcemap.json && wally-package-types --sourcemap sourcemap.json Packages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chokidar-cli": "^3.0.0"
  }
}
